# Package Managers
package-lock.json
pnpm-lock.yaml

# Build output and coverage (generated)
dist
coverage

# Local tooling / scratch
.claude
.gemini

# Scanner fixtures are intentionally raw source samples (incl. a cast escape
# hatch); keep them verbatim so the scanner sees exactly what users write.
test/fixtures